"""Diannot Studio — the multi-page NiceGUI front-end (the `diannot studio` app).

A thin presentation layer that composes the existing, decoupled backend (render,
ingest pipeline, cards/srs, quiz, glossary, search, export). Runs as a native
desktop window or in a browser from one codebase. Launch via `launch_studio()`.
"""
